---
title: Create a serverless API with TypeScript, GraphQL and MongoDB
date: "2019-11-20"
description: "Let's create a Serverless GraphQL API and deploy it using Vercel"
banner: "/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/banner.png"
updated: "2020-12-04"
---

> Updated on 4th December, 2020 to update [ZEIT's change to Vercel](https://vercel.com/blog/zeit-is-now-vercel).

> Updated on 30th July, 2020 to show that this can be used in [Next.js API Routes](https://nextjs.org/docs/api-routes/introduction).

> Updated on 3rd May, 2020 to reflect the changes on how Vercel handles [Environment Variables](https://vercel.com/blog/environment-variables-ui).

<SpecialBlock type="info">
  If you would like to use{" "}
  <a href="https://www.prisma.io/" target="_blank" rel="noopener noreferrer">
    Prisma
  </a>{" "}
  instead of MongoDB, check out my post{" "}
  <a
    href="/serverless-graphql-api-typescript-prisma-postgressql"
    target="_blank"
    rel="noopener noreferrer"
  >
    here
  </a>
  .
</SpecialBlock>

In this post we'll create a GraphQL API to create, view and delete notes that we can deploy to [Vercel](https://vercel.com/home) using [Apollo Server](https://www.apollographql.com/docs/apollo-server/), TypeScript and MongoDB.

## Initial Setup

First [Vercel](https://vercel.com/download) will need be downloaded, installed and logged into.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/initial-setup/install-vercel.png"
  width={1040}
  height={560}
  title="Installing Vercel"
  fullBleed={false}
/>

The nice thing about using **Vercel** for development is that we don't have to worry about manually configuring TypeScript.

Then create a folder, initialize a project in it and install `typescript` as a dev dependency.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/initial-setup/initialize-project.png"
  width={1040}
  height={548}
  title="Initialize Project"
  fullBleed={false}
/>

## Setup the Database

To start setting up the database first go to [MongoDB Atlas](https://www.mongodb.com/), create a database and get the connection URI.

> If you want a more detailed explanation check out my [previous guide here](https://akhilaariyachandra.com/setup-mongodb-in-nodejs-with-mongoose/). Note that you can't follow it exactly here because you can no longer create new accounts in MLab (use [MongoDB Atlas](https://www.mongodb.com/) instead) and that guide is for an always running server, not a serverless one.

Then install the `mongoose` dependency. Since we'll be working with TypeScript we'll need the types for `mongoose` as well.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/install-mongoose.png"
  width={1040}
  height={416}
  title="Installing Mongoose"
  fullBleed={false}
/>

After let's create the model for the notes. Create a folder called `src`. After that create a folder called `database` in `src`. Then in that create another folder called `models` and in that create the file `note.ts`.

Start by importing `mongoose`.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/note.ts-1.png"
  width={1040}
  height={372}
  title="Importing Mongoose"
  fullBleed={false}
/>

Next create an interface for the model which extends from `mongoose.Document`. We'll need to export this interface for use later as well.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/note.ts-2.png"
  width={1040}
  height={640}
  title="Creating the Interface"
  fullBleed={false}
/>

After that we'll create the schema definition.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/note.ts-3.png"
  width={1282}
  height={640}
  title="Schema Definition"
  fullBleed={false}
/>

Then define the name of the collection and the schema using schema definition.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/note.ts-4.png"
  width={1264}
  height={504}
  title="Collection and Schema name"
  fullBleed={false}
/>

Since we're building a serverless API, we can't depend on a persistent connection to database. When creating a Model we need to create it using the database connection.

To compensate for using serverless we'll generate the Model at runtime using a function. After declaring the function, make it the default export.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/note.ts-5.png"
  width={1296}
  height={592}
  title="Model Connection"
  fullBleed={false}
/>

In the end your `note.ts` file should look like this.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/note.ts-6.png"
  width={1298}
  height={1268}
  title="Note Model full"
  fullBleed={false}
/>

After creating the Note Model, let's look into the database connection.

Since we're using serverless, we can't have a persistent connection to database for every invocation of the serverless function as this would create issues with scaling.

First create the `.env` file in the project root to store the MongoDB URI as `DB_PATH`.

<SpecialBlock type="warn">
  Never commit the <strong>.env</strong> file. Make sure to add it to
  <strong>.gitignore</strong>.
</SpecialBlock>

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/env.png"
  width={1296}
  height={368}
  title="Environment File"
  fullBleed={false}
/>

Next create the the `index.js` file in `src/database` and import `mongoose`.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/index.ts-1.png"
  width={1040}
  height={368}
  title="Importing Mongoose in Database file"
  fullBleed={false}
/>

Then get the MongoDB URI that we included in the **.env** file. It can be accessed as a key in `process.env`.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/index.ts-2.png"
  width={1040}
  height={460}
  title="Getting the MongoDB URI"
  fullBleed={false}
/>

After that declare a variable that will be used to cache the database connection between function invocations to prevent overloading the database.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/index.ts-3.png"
  width={1040}
  height={548}
  title="Caching the Connection"
  fullBleed={false}
/>

Finally we need to create a function that will return a database connection. The function will first check if there is a cached connection. If there is one it will return it or else it will create a new connection and cache and return it. Be sure to export the function.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-the-database/index.ts-4.png"
  width={1396}
  height={1178}
  title="Full code of index.ts"
  fullBleed={false}
/>

## Setup the GraphQL API

First let's install the dependencies.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/install-dependencies.png"
  width={1184}
  height={372}
  title="Installing GraphQL dependencies"
  fullBleed={false}
/>

Next create the folder `api` in the project root and in it create the file `graphql.ts`. **Vercel** will expose the serverless function in the file as the endpoint **/api/graphql**. The GraphQL function will be created using [Apollo Server](https://www.apollographql.com/docs/apollo-server/). Since we're doing a serverless version we'll be using [apollo-server-micro](https://www.npmjs.com/package/apollo-server-micro).

Then import `ApolloServer` from `apollo-server-micro` and the function to create the database connection.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/graphql.ts-1.png"
  width={1050}
  height={412}
  title="Importing Dependencies"
  fullBleed={false}
/>

After that import the GraphQL Schemas and Resolvers. We'll create these later.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/graphql.ts-2.png"
  width={1040}
  height={508}
  title="Importing Schemas and Resolvers"
  fullBleed={false}
/>

Then initialize the Apollo Server and export it.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/graphql.ts-3.png"
  width={1332}
  height={684}
  title="Initializing the Apollo Server"
  fullBleed={false}
/>

When creating a new `ApolloServer`, we can define the `context` option. The `context` is an object which can be accessed by any of the resolvers.

This will be a good place to initialize the database connection and automatically pass it down to the resolvers.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/graphql.ts-4.png"
  width={1040}
  height={908}
  title="Adding Context to the Apollo Server"
  fullBleed={false}
/>

To finish configuring we'll set the `introspection` and `playground` options.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/graphql.ts-5.png"
  width={1040}
  height={1044}
  title="Enabling Playground and Introspection"
  fullBleed={false}
/>

In the end the `graphql.ts` file should look like this.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql/graphql.ts-6.png"
  width={1332}
  height={1178}
  title="Full graphql.ts file"
  fullBleed={false}
/>

## Setup the GraphQL Schema

In the `src` folder create the `graphql` folder and in it create the `schema` folder.

GraphQL doesn't have a type for date and time so we'll need a schema for those types. Create `custom.ts` in the `schema` folder.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/custom.ts.png"
  width={1040}
  height={636}
  title="Custom Schema file"
  fullBleed={false}
/>

Next we'll setup the schema for Note.

First create the Note schema file, `note.ts` in the `schema` folder.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/note.ts-1.png"
  width={1040}
  height={460}
  title="Import dependency"
  fullBleed={false}
/>

Then add the Note type.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/note.ts-2.png"
  width={1040}
  height={774}
  title="Adding Note type"
  fullBleed={false}
/>

After that add two Queries, one to get all Notes and one to get a specific Note.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/note.ts-3.png"
  width={1040}
  height={998}
  title="Adding Queries"
  fullBleed={false}
/>

Next add two Mutations to create new Notes and delete existing ones.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/note.ts-4.png"
  width={1084}
  height={1222}
  title="Adding Mutations"
  fullBleed={false}
/>

> Note that we add the keyword `extend` to the Query and Mutation type in the two schemas. This is because we'll be joining all the schemas into one later.

Finally we'll need a schema a join all the others schemas together. Create `index.ts` in the `schema` folder and declare and export the Link Schema in an array.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/index.ts-1.png"
  width={1040}
  height={1088}
  title="Joining Schemas"
  fullBleed={false}
/>

Then import the other Schemas and add them to the array.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-schema/index.ts-2.png"
  width={1100}
  height={1222}
  title="Adding Schemas"
  fullBleed={false}
/>

## Setup the resolvers

Since we added a custom type for dates and times in our schema, the first resolver we'll add is for those types. Create a folder called `resolvers` in the `graphql` folder and in it create `custom.ts`.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/custom.ts.png"
  width={1480}
  height={640}
  title="Custom Resolvers"
  fullBleed={false}
/>

Next we'll start working on the resolver for Notes.

Start by creating `note.ts` in `graphql`, importing the required dependencies and exporting an empty object which is going to be our resolver.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-1.png"
  width={1232}
  height={594}
  title="Importing dependencies in Note resolvers"
  fullBleed={false}
/>

Then define an object for the Queries.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-2.png"
  width={1040}
  height={548}
  title="Query Object"
  fullBleed={false}
/>

When defining the function for each of the queries (or mutations or fields) there are three important parameters.

- `parent` - If you a resolving a field of an object this parameter will contain it. We won't be needing it in this post.
- `args` - The arguments passed to the query or mutation.
- `context` - The context object created when setting up the API. In our case it'll contain the database connection.

First let's write the resolver for `getAllNotes`. Start by declaring the function.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-3.png"
  width={1040}
  height={730}
  title="Arguments for getAllNotes"
  fullBleed={false}
/>

Let's destructure the `context` object.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-4.png"
  width={1040}
  height={730}
  title="Destructure context object"
  fullBleed={false}
/>

In the function we should create the Note model using the database connection from the context and retrieve all the Notes using it.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-5.png"
  width={1198}
  height={1146}
  title="Get all Notes"
  fullBleed={true}
/>

After that let's add the resolver for `getNote`.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-6.png"
  width={1198}
  height={1146}
  title="Adding resolver for getNote"
  fullBleed={true}
/>

We can retrieve the `id` argument from the second parameter.

Let's finish of the Note resolvers by adding the resolvers for the mutations.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-7.png"
  width={1512}
  height={2432}
  title="Adding resolvers for Mutations"
  fullBleed={true}
/>

In the end `note.ts` should look like this.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/note.ts-8.png"
  width={1512}
  height={4268}
  title="Full note.ts file"
  fullBleed={true}
/>

Now we have to connect all the resolvers together using an `index.ts` file created in the `resolvers` folder.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/index.ts.png"
  width={1040}
  height={508}
  title="Resolver Index"
  fullBleed={false}
/>

## Running the API locally

To run the project locally first you need to link it to a **Vercel** project.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/shell-1.png"
  width={1040}
  height={370}
  title="Linking to Vercel project"
  fullBleed={false}
/>

Then run `vercel dev` to start the API locally.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/shell-2.png"
  width={1040}
  height={370}
  title="Running Vercel locally"
  fullBleed={false}
/>

If you visit `http://localhost:3000/api/graphql` you can see the GraphQL Playground.

## Deploying to [Vercel](https://vercel.com/home)

First we need to upload the database path as an [Environment Variable](https://vercel.com/blog/environment-variables-ui).

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/shell-3.png"
  width={1040}
  height={370}
  title="Adding Environment Variables"
  fullBleed={false}
/>

Name the variable `DB_PATH` and make sure you make to available for all three environments (**Production**, **Preview** and **Development**).

Then all that's left to do is to deploy to _Vercel_.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/shell-4.png"
  width={1040}
  height={370}
  title="Deploying to Vercel"
  fullBleed={false}
/>

The GraphQL Playground should be visible in the `/api/graphql` route of the URL returned.

## Wrapping Up

I made a sample deployment which you can check out below or [here](https://serverless-graphql-api-example-five-gamma.now.sh/api/graphql). The source code is available on [GitHub](https://github.com/akhila-ariyachandra/serverless-graphql-api-example).

<Iframe url="https://serverless-graphql-api-example-five-gamma.now.sh/api/graphql" />

If you want a more detailed explanation into GraphQL and making a server for it, you can check out the excellent guide that I learnt from [here](https://www.robinwieruch.de/graphql-apollo-server-tutorial).

## Bonus: Using it in [Next.js](https://nextjs.org/)

You can create a GraphQL API endpoint in your Next.js project by putting the `graphql.ts` inside the `api` folder in the `pages` folder.

The only extra step you need to do is to add this bit of code to the end of the `graphql.ts` file.

<PostImage
  src="/blog/create-a-serverless-api-with-typescript-graphql-and-mongodb/setup-graphql-resolvers/graphql.ts.png"
  width={1332}
  height={730}
  title="Using in Next.js"
  fullBleed={false}
/>
